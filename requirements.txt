# ATO detection pipeline - pinned versions for reproducibility.
# These are the versions the leak-free (v2) pipeline was built and tested with.
# Install with:  python3 -m pip install -r requirements.txt

numpy==1.24.3
pandas==2.3.3
scikit-learn==1.8.0
tensorflow==2.20.0
imbalanced-learn==0.14.1
matplotlib==3.10.8
pyarrow==23.0.1
shap>=0.44

# dev/test only
pytest>=8.0
ruff>=0.4
